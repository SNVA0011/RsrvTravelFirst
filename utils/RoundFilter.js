export const Roundreducer = (state, action) => {
  console.log("stat=============", state);
  console.log("action", action);
  switch (action.type) {
    case "price":
      return state;
    default:
      state;
  }
};
